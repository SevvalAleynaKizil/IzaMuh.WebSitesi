
<div class="container">
    <a href="/Admin/Fotograf/Insert" class="btn btn-primary" style="margin-bottom:15px">Yeni Fotoğraf Ekle</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <td>#</td>
                <td>Fotoğraf</td>
            </tr>

        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td style="text-align:center"><img src="/images/@item.Name" width="50"/></td>
                   
                    <td>
                        <a href="/Admin/Fotograf/Update/@item.Id" class="btn btn-primary">Güncelle</a>
                        <a href="/Admin/Fotograf/Delete/@item.Id" class="btn btn-success">Sil</a>

                    </td>

                </tr>
            }

        </tbody>

    </table>
</div>